using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VG : MonoBehaviour
{

    // Estas son las variables que accederan los diferentes Scripts para modificarlos segun vayamos avanzando en el juego
    public static int score = 0;
    public static int healthPlayerStatic;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
